<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:FoodApp.ViewModels"
             xmlns:models="clr-namespace:FoodApp.Models"
             x:Class="FoodApp.Views.NearbyPage"
             x:DataType="vm:NearbyViewModel"
             Style="{StaticResource PageStyle}"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto,Auto,*" Padding="16" RowSpacing="12">
        <Button Grid.Row="0"
                Text="Refresh location"
                Style="{StaticResource PrimaryButtonStyle}"
                Command="{Binding RefreshCommand}"
                IsEnabled="{Binding IsLocationAvailable}"
                AutomationProperties.Name="Refresh GPS location" />

        <Label Grid.Row="1" Text="{Binding LocationStatus}" Style="{StaticResource CaptionStyle}" />

        <CollectionView Grid.Row="2" ItemsSource="{Binding Places}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:NearbyPlace">
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,4">
                        <VerticalStackLayout Padding="8">
                            <Label Text="{Binding Name}" FontAttributes="Bold" />
                            <Label Text="{Binding Address}" Style="{StaticResource CaptionStyle}" />
                            <Label Text="{Binding DistanceKm, StringFormat='{0:F1} km away'}" />
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
